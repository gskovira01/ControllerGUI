# TIM Motion Service Requirements
# =================================
# Install with: pip install -r requirements.txt

# Core dependencies
pyyaml>=6.0              # Configuration file parsing
# [CHANGE 2026-04-11 12:50:00 -05:00] RSI RapidCode Python extension requires NumPy 1.x ABI
numpy>=1.22.0,<2         # RapidCode requirement (if using real hardware)

# Optional: for real RapidCode (installed separately from RSI)
# RSI RapidCode should be installed at C:\RSI\10.7.1 (Windows) or equivalent

# Testing & development
pytest>=7.0              # Unit testing
pytest-cov>=3.0          # Coverage reporting

# Logging & monitoring
python-daemon>=2.3       # For Windows Service integration
pywin32>=300             # Windows-specific tools (for service installation)
